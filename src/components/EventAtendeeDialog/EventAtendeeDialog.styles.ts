import { makeStyles } from "@mui/styles";
import { appFontFamily, designTokens } from "../../theme/tokens";

const { color, motion, radius, shadow } = designTokens;

export const useStyles = makeStyles({
  dialogPaper: {
    width: 1120,
    maxWidth: "96vw",
    borderRadius: radius.dialog,
    overflow: "hidden",
    direction: "rtl" as const,
    fontFamily: appFontFamily,
    animation: `scaleIn ${motion.slow} ${motion.drawer}`,
    boxShadow: shadow.lg,
    backgroundColor: color.canvas,
    border: `1px solid ${color.border}`,
  },
  header: {
    position: "relative" as const,
    color: color.text,
    padding: "22px 24px 14px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    fontWeight: 800,
    fontSize: 21,
    fontFamily: appFontFamily,
    backgroundColor: color.canvas,
  },
  closeButton: {
    position: "absolute" as const,
    left: 12,
    top: 14,
    color: color.textSecondary,
    backgroundColor: "rgba(29, 29, 31, 0.06)",
    transition: `transform ${motion.fast} ${motion.easeOut}, background-color ${motion.fast} ease`,
    "&:hover": {
      backgroundColor: "rgba(29, 29, 31, 0.10)",
    },
  },
  addButton: {
    position: "absolute" as const,
    right: 12,
    top: 14,
    color: color.primary,
    backgroundColor: color.primarySoft,
    border: `1px solid rgba(47, 111, 97, 0.20)`,
    transition: `transform ${motion.fast} ${motion.easeOut}, background-color ${motion.fast} ease`,
    "&:hover": {
      backgroundColor: color.primarySoft,
      transform: "translateY(-1px)",
    },
  },
  sendAssignmentsButtonWrap: {
    position: "absolute" as const,
    right: 62,
    top: 14,
    display: "inline-flex",
  },
  sendAssignmentsButton: {
    color: color.brand,
    backgroundColor: color.brandSoft,
    border: `1px solid rgba(111, 78, 124, 0.20)`,
    transition: `transform ${motion.fast} ${motion.easeOut}, background-color ${motion.fast} ease`,
    "&:hover": {
      backgroundColor: color.brandSoft,
      transform: "translateY(-1px)",
    },
    "&.Mui-disabled": {
      color: color.textMuted,
      backgroundColor: color.surfaceMuted,
    },
  },
  printSheetButtonWrap: {
    position: "absolute" as const,
    right: 112,
    top: 14,
    display: "inline-flex",
  },
  printSheetIconButton: {
    color: color.info,
    backgroundColor: color.infoSoft,
    border: `1px solid rgba(37, 99, 235, 0.18)`,
    transition: `transform ${motion.fast} ${motion.easeOut}, background-color ${motion.fast} ease`,
    "&:hover": {
      backgroundColor: color.infoSoft,
      transform: "translateY(-1px)",
    },
    "&.Mui-disabled": {
      color: color.textMuted,
      backgroundColor: color.surfaceMuted,
    },
  },
  dialogContent: {
    direction: "rtl" as const,
    maxHeight: "min(72dvh, 680px)",
    padding: "14px 20px 20px",
    overflow: "hidden",
    backgroundColor: color.canvas,
    "&::-webkit-scrollbar": {
      width: 6,
    },
    "&::-webkit-scrollbar-thumb": {
      backgroundColor: "rgba(47, 111, 97, 0.22)",
      borderRadius: 3,
    },
  },
  dialogToolbar: {
    display: "flex",
    justifyContent: "flex-start",
    alignItems: "center",
    marginBottom: 14,
  },
  printSheetButton: {
    direction: "rtl" as const,
    color: color.primary,
    borderColor: color.border,
    backgroundColor: color.surface,
    border: `1px solid ${color.border}`,
    borderRadius: radius.control,
    padding: "7px 14px",
    fontFamily: appFontFamily,
    fontWeight: 700,
    fontSize: 13,
    textTransform: "none" as const,
    boxShadow: "0 3px 10px rgba(45, 35, 43, 0.05)",
    "& .MuiButton-startIcon": {
      marginLeft: 8,
      marginRight: 0,
    },
    "&:hover": {
      backgroundColor: color.primarySoft,
      borderColor: color.primary,
    },
    "&.Mui-disabled": {
      color: color.textMuted,
      borderColor: color.borderSubtle,
      backgroundColor: color.surfaceMuted,
    },
  },
  participantsGrid: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) minmax(0, 1fr) minmax(0, 1fr)",
    gap: 16,
    direction: "ltr" as const,
    minHeight: 0,
    height: "min(62dvh, 560px)",
  },
  sectionColumn: {
    direction: "rtl" as const,
    minWidth: 0,
    minHeight: 0,
    display: "flex",
    flexDirection: "column" as const,
    backgroundColor: color.surface,
    border: `1px solid ${color.borderSubtle}`,
    borderRadius: radius.panel,
    boxShadow: shadow.sm,
    overflow: "hidden",
  },
  sectionBody: {
    minHeight: 0,
    flex: 1,
    overflowY: "auto" as const,
    padding: "12px",
    "&::-webkit-scrollbar": {
      width: 6,
    },
    "&::-webkit-scrollbar-thumb": {
      backgroundColor: "rgba(47, 111, 97, 0.18)",
      borderRadius: 3,
    },
  },
  pairedCardsGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))",
    alignContent: "start",
    alignItems: "stretch",
    gap: 10,
  },
  listItem: {
    padding: "10px 14px",
    borderRadius: 14,
    marginBottom: 8,
    backgroundColor: color.surface,
    border: `1px solid ${color.borderSubtle}`,
    transition: `transform ${motion.fast} ${motion.easeOut}, background-color ${motion.fast} ease, box-shadow ${motion.fast} ease`,
    animation: `fadeInUp ${motion.slow} ${motion.easeOut} both`,
    display: "flex",
    alignItems: "center",
    gap: 12,
    "&:hover": {
      backgroundColor: color.primarySoft,
      transform: "translateX(4px)",
      boxShadow: shadow.sm,
    },
    "&:last-child": {
      marginBottom: 0,
    },
  },
  pairRow: {
    padding: "7px 8px",
    borderRadius: 12,
    backgroundColor: color.surface,
    border: `1px solid ${color.borderSubtle}`,
    display: "grid",
    gridTemplateColumns: "minmax(0, 1.2fr) auto minmax(0, 0.8fr)",
    alignItems: "center",
    gap: 8,
    minWidth: 0,
    height: "100%",
    direction: "rtl" as const,
    transition:
      `border-color ${motion.normal} ease, background-color ${motion.normal} ease, box-shadow ${motion.normal} ease`,
  },
  pairedTraineeButton: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    gap: 8,
    padding: "5px 6px",
    borderRadius: 10,
    border: "1px solid transparent",
    color: color.text,
    backgroundColor: color.primarySoft,
    textTransform: "none" as const,
    minWidth: 0,
    "&:hover": {
      backgroundColor: color.primarySoft,
      borderColor: color.primary,
    },
    "&.Mui-disabled": {
      color: color.textMuted,
      backgroundColor: color.surfaceMuted,
      opacity: 0.72,
    },
  },
  availablePairingTarget: {
    cursor: "pointer",
    borderColor: color.primary,
    backgroundColor: color.primarySoft,
    boxShadow: "0 0 0 2px rgba(47, 111, 97, 0.10)",
    "&:hover": {
      borderColor: color.primaryHover,
      backgroundColor: color.primarySoft,
      boxShadow: shadow.sm,
    },
    "&:focus-visible": {
      outline: `2px solid ${color.primary}`,
      outlineOffset: 2,
    },
  },
  pairingTargetIcon: {
    marginInlineStart: "auto",
    flexShrink: 0,
    color: color.primary,
    opacity: 0,
  },
  pairingTargetIconVisible: {
    opacity: 1,
  },
  pairedMentorsList: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 6,
    minWidth: 0,
  },
  pairedMentorRow: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto",
    alignItems: "center",
    gap: 6,
    minWidth: 0,
    padding: "4px 5px",
    borderRadius: 9,
    backgroundColor: color.surface,
    border: `1px solid ${color.borderSubtle}`,
  },
  pairDirection: {
    color: color.primary,
    fontSize: 16,
    lineHeight: 1,
    fontFamily: appFontFamily,
  },
  pairedMentorInfo: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    minWidth: 0,
  },
  mentorAvatar: {
    width: 30,
    height: 30,
    flexShrink: 0,
    backgroundColor: color.primarySoft,
    color: color.primary,
    fontSize: 12,
    fontWeight: 700,
    fontFamily: appFontFamily,
  },
  selectableItem: {
    width: "100%",
    padding: "8px 10px",
    borderRadius: 10,
    marginBottom: 0,
    backgroundColor: "transparent",
    border: "1px solid transparent",
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    gap: 10,
    color: color.text,
    textTransform: "none" as const,
    transition: `background-color ${motion.fast} ease, border-color ${motion.fast} ease, transform ${motion.fast} ${motion.easeOut}`,
    minWidth: 0,
    "&:hover": {
      backgroundColor: color.primarySoft,
      borderColor: color.primary,
      transform: "translateY(-1px)",
    },
  },
  selectedItem: {
    backgroundColor: `${color.primarySoft} !important`,
    borderColor: `${color.primary} !important`,
    boxShadow: "0 0 0 2px rgba(47, 111, 97, 0.14)",
  },
  attendeeRow: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto",
    alignItems: "center",
    gap: 12,
    marginBottom: 10,
    padding: 8,
    borderRadius: 12,
    backgroundColor: color.surface,
    border: `1px solid ${color.borderSubtle}`,
  },
  participantActions: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    paddingInlineStart: 4,
  },
  sectionTitle: {
    margin: 0,
    padding: "12px 14px",
    color: color.textSecondary,
    fontFamily: appFontFamily,
    fontWeight: 800,
    fontSize: 14,
    backgroundColor: color.surfaceMuted,
    borderBottom: `1px solid ${color.borderSubtle}`,
  },
  sectionEmpty: {
    margin: "4px 2px 8px",
    color: color.textMuted,
    fontFamily: appFontFamily,
    fontSize: 12,
  },
  personName: {
    maxWidth: "100%",
    color: color.text,
    fontFamily: appFontFamily,
    fontWeight: 700,
    fontSize: 13,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    minWidth: 0,
  },
  personMeta: {
    maxWidth: "100%",
    color: color.textSecondary,
    fontFamily: appFontFamily,
    fontSize: 11,
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap" as const,
    minWidth: 0,
  },
  loadingState: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    minHeight: 200,
  },
  attendeeName: {
    textAlign: "right" as const,
    fontFamily: appFontFamily,
    "& .MuiListItemText-primary": {
      fontWeight: 600,
      fontSize: 14,
      color: color.text,
    },
    "& .MuiListItemText-secondary": {
      fontSize: 12,
      color: color.textMuted,
    },
  },
  avatar: {
    width: 38,
    height: 38,
    backgroundColor: color.primarySoft,
    color: color.primary,
    fontSize: 14,
    fontWeight: 700,
    fontFamily: appFontFamily,
    transition: `transform ${motion.normal} ${motion.easeOut}`,
  },
  deleteButton: {
    width: 44,
    height: 44,
    flexShrink: 0,
    color: color.danger,
    backgroundColor: color.dangerSoft,
    border: "1px solid rgba(180, 35, 24, 0.20)",
    transition: `transform ${motion.fast} ${motion.easeOut}, background-color ${motion.fast} ease, border-color ${motion.fast} ease`,
    "&:hover": {
      color: color.danger,
      backgroundColor: color.dangerSoft,
      borderColor: color.danger,
      transform: "translateY(-1px)",
    },
  },
  emptyState: {
    display: "flex",
    flexDirection: "column" as const,
    alignItems: "center",
    justifyContent: "center",
    padding: "40px 20px",
    color: color.textMuted,
    fontFamily: appFontFamily,
  },
  countBadge: {
    fontFamily: appFontFamily,
    fontSize: 12,
    fontWeight: 600,
    color: color.textSecondary,
    marginTop: 2,
  },
  printPreviewDialogPaper: {
    width: 860,
    maxWidth: "94vw",
    borderRadius: radius.dialog,
    overflow: "hidden",
    fontFamily: appFontFamily,
    direction: "rtl" as const,
    backgroundColor: color.canvas,
    border: `1px solid ${color.border}`,
  },
  printPreviewHeader: {
    position: "relative" as const,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "18px 20px",
    color: color.text,
    backgroundColor: color.canvas,
  },
  printPreviewTitle: {
    fontFamily: appFontFamily,
    fontWeight: 800,
    fontSize: 20,
  },
  printPreviewCloseButton: {
    position: "absolute" as const,
    left: 12,
    top: 12,
    color: color.textSecondary,
    backgroundColor: "rgba(29, 29, 31, 0.06)",
    "&:hover": {
      backgroundColor: "rgba(29, 29, 31, 0.10)",
    },
  },
  printDialogContent: {
    maxHeight: "min(74dvh, 760px)",
    overflowY: "auto" as const,
    padding: "20px",
    backgroundColor: color.canvas,
  },
  printPreviewActions: {
    direction: "rtl" as const,
    padding: "12px 18px 16px",
    gap: 10,
    backgroundColor: color.surface,
    borderTop: `1px solid ${color.borderSubtle}`,
    "& .MuiButton-startIcon": {
      marginLeft: 8,
      marginRight: 0,
    },
  },
  printPrimaryButton: {
    backgroundColor: color.primary,
    color: "#fff",
    borderRadius: radius.control,
    padding: "7px 14px",
    fontFamily: appFontFamily,
    fontWeight: 700,
    textTransform: "none" as const,
    "&:hover": {
      backgroundColor: color.primaryHover,
    },
  },
  printSecondaryButton: {
    color: color.textSecondary,
    borderRadius: radius.control,
    padding: "7px 14px",
    fontFamily: appFontFamily,
    fontWeight: 700,
    textTransform: "none" as const,
    "&:hover": {
      backgroundColor: color.primarySoft,
    },
  },
  shabbatSheet: {
    direction: "rtl" as const,
    color: "#111",
    backgroundColor: "#fff",
    width: "100%",
    maxWidth: 780,
    margin: "0 auto",
    padding: "28px 34px",
    borderRadius: 8,
    border: "1px solid #e6e0e4",
    boxShadow: "0 8px 28px rgba(20, 15, 18, 0.08)",
    fontFamily: "inherit",
  },
  printTitleBlock: {
    textAlign: "center" as const,
    marginBottom: 22,
    paddingBottom: 14,
    borderBottom: "2px solid #111",
  },
  printOrgTitle: {
    fontFamily: "inherit",
    fontSize: 18,
    fontWeight: 700,
    color: "#111",
    marginBottom: 4,
  },
  printDocumentTitle: {
    fontFamily: "inherit",
    fontSize: 30,
    fontWeight: 800,
    color: "#111",
  },
  printEventInfoGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: "10px 18px",
    marginBottom: 24,
    padding: 14,
    border: "1px solid #d8d3d6",
    borderRadius: 6,
    backgroundColor: "#fafafa",
  },
  printEventInfoItem: {
    display: "grid",
    gridTemplateColumns: "86px minmax(0, 1fr)",
    alignItems: "baseline",
    gap: 8,
    minWidth: 0,
  },
  printInfoLabel: {
    fontFamily: "inherit",
    fontSize: 12,
    fontWeight: 700,
    color: "#555",
  },
  printInfoValue: {
    fontFamily: "inherit",
    fontSize: 13,
    fontWeight: 500,
    color: "#111",
    overflowWrap: "anywhere" as const,
  },
  printShirtSizeSummary: {
    display: "flex",
    flexDirection: "column" as const,
    alignItems: "stretch",
    gap: 6,
    marginBottom: 20,
    padding: "10px 14px",
    border: "1px solid #d8d3d6",
    borderRadius: 6,
    backgroundColor: "#fafafa",
    breakInside: "avoid" as const,
    pageBreakInside: "avoid" as const,
  },
  printShirtSizeSummaryTitle: {
    flexShrink: 0,
    fontFamily: "inherit",
    fontSize: 13,
    fontWeight: 800,
    color: "#111",
  },
  printShirtSizeSummaryItems: {
    display: "flex",
    flex: 1,
    flexWrap: "wrap" as const,
    gap: "4px 18px",
    minWidth: 0,
  },
  printShirtSizeSummaryItem: {
    fontFamily: "inherit",
    fontSize: 12.5,
    fontWeight: 500,
    color: "#222",
    overflowWrap: "anywhere" as const,
  },
  printSection: {
    marginTop: 18,
    breakInside: "avoid" as const,
    pageBreakInside: "avoid" as const,
  },
  printSectionTitle: {
    fontFamily: "inherit",
    fontSize: 17,
    fontWeight: 800,
    color: "#111",
    marginBottom: 8,
    paddingBottom: 6,
    borderBottom: "1px solid #222",
  },
  printTable: {
    width: "100%",
    borderCollapse: "collapse" as const,
    tableLayout: "fixed" as const,
    "& .MuiTableCell-root": {
      border: "1px solid #222",
      padding: "7px 10px",
      fontFamily: "inherit",
      fontSize: 13,
      color: "#111",
      textAlign: "right" as const,
      backgroundColor: "#fff",
    },
    "& .MuiTableHead-root .MuiTableCell-root": {
      fontWeight: 800,
      backgroundColor: "#f0f0f0",
    },
    "& .MuiTableRow-root": {
      breakInside: "avoid" as const,
      pageBreakInside: "avoid" as const,
    },
  },
  printParticipantDetails: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 2,
    minWidth: 0,
  },
  printPairedMentors: {
    display: "flex",
    flexDirection: "column" as const,
    gap: 7,
    "& > * + *": {
      paddingTop: 7,
      borderTop: "1px solid #ddd",
    },
  },
  printParticipantName: {
    fontFamily: "inherit",
    fontSize: 13,
    fontWeight: 700,
    lineHeight: 1.35,
    color: "#111",
  },
  printParticipantMeta: {
    fontFamily: "inherit",
    fontSize: 11.5,
    fontWeight: 400,
    lineHeight: 1.35,
    color: "#333",
    overflowWrap: "anywhere" as const,
  },
  printParticipantLabel: {
    fontWeight: 700,
  },
  printEmptyText: {
    fontFamily: "inherit",
    fontSize: 13,
    color: "#555",
    border: "1px solid #d8d3d6",
    borderRadius: 6,
    padding: "10px 12px",
    backgroundColor: "#fafafa",
  },
  printHost: {
    display: "none",
  },
  "@media (prefers-reduced-motion: reduce)": {
    dialogPaper: {
      animation: "none",
    },
    listItem: {
      animation: "none",
      transition: "background-color 120ms ease, border-color 120ms ease",
      "&:hover": {
        transform: "none",
      },
    },
    selectableItem: {
      transition: "background-color 120ms ease, border-color 120ms ease",
      "&:hover": {
        transform: "none",
      },
    },
    deleteButton: {
      transition: "background-color 120ms ease, border-color 120ms ease",
      "&:hover": {
        transform: "none",
      },
    },
    addButton: {
      transition: "background-color 120ms ease",
      "&:hover": {
        transform: "none",
      },
    },
    sendAssignmentsButton: {
      transition: "background-color 120ms ease",
      "&:hover": {
        transform: "none",
      },
    },
    printSheetIconButton: {
      transition: "background-color 120ms ease",
      "&:hover": {
        transform: "none",
      },
    },
  },
  "@media print": {
    "@page": {
      size: "A4 portrait",
      margin: "14mm",
    },
    "@global": {
      "html, body": {
        width: "auto !important",
        minHeight: "auto !important",
        margin: "0 !important",
        overflow: "visible !important",
        background: "#fff !important",
      },
      "body > *:not(.shabbat-print-host)": {
        display: "none !important",
      },
      ".shabbat-print-host, .shabbat-print-host *": {
        visibility: "visible !important",
      },
      ".shabbat-print-host": {
        display: "block !important",
        position: "static !important",
        width: "100% !important",
        minHeight: "0 !important",
        overflow: "visible !important",
      },
      ".shabbat-print-root": {
        position: "static !important",
        width: "100% !important",
        maxWidth: "none !important",
        margin: "0 !important",
        padding: "0 !important",
        border: "0 !important",
        boxShadow: "none !important",
        borderRadius: "0 !important",
      },
      ".MuiBackdrop-root, .MuiDialogActions-root": {
        display: "none !important",
      },
    },
    printHost: {
      display: "block !important",
    },
    printPreviewDialogPaper: {
      boxShadow: "none !important",
      backgroundColor: "#fff !important",
      width: "100% !important",
      maxWidth: "none !important",
      margin: "0 !important",
    },
    printPreviewHeader: {
      display: "none !important",
    },
    printDialogContent: {
      padding: "0 !important",
      margin: "0 !important",
      overflow: "visible !important",
      maxHeight: "none !important",
      backgroundColor: "#fff !important",
    },
    printPreviewActions: {
      display: "none !important",
    },
    printEventInfoGrid: {
      backgroundColor: "#fff !important",
    },
    printShirtSizeSummary: {
      backgroundColor: "#fff !important",
    },
    printEmptyText: {
      backgroundColor: "#fff !important",
    },
    printSection: {
      breakInside: "avoid-page" as const,
      pageBreakInside: "avoid" as const,
    },
  },
  "@media (max-width: 960px)": {
    dialogPaper: {
      width: "calc(100vw - 24px)",
      maxWidth: "calc(100vw - 24px)",
    },
    dialogContent: {
      maxHeight: "72dvh",
      overflowY: "auto" as const,
      padding: "14px",
    },
    participantsGrid: {
      gridTemplateColumns: "1fr",
      height: "auto",
      direction: "rtl" as const,
    },
    sectionBody: {
      maxHeight: 320,
    },
    printPreviewDialogPaper: {
      width: "calc(100vw - 20px)",
      maxWidth: "calc(100vw - 20px)",
    },
    printDialogContent: {
      padding: "12px",
    },
    shabbatSheet: {
      padding: "22px 18px",
    },
    printEventInfoGrid: {
      gridTemplateColumns: "1fr",
    },
  },
});
